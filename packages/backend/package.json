{
  "name": "line-bot-farm-backend",
  "version": "1.0.0",
  "description": "Cloudflare Workers + Hono Backend for XingNong Farm Service Request",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init": "wrangler d1 execute xingnong-db --local --file=schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.6.0",
    "wrangler": "4.139.0"
  }
}
